<%= form_for(@texto_vinetum) do |f| %>
  <% if @texto_vinetum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@texto_vinetum.errors.count, "error") %> prohibited this texto_vinetum from being saved:</h2>

      <ul>
      <% @texto_vinetum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tve_texto %><br>
    <%= f.text_area :tve_texto %>
  </div>
  <div class="field">
    <%= f.label :texto_id %><br>
    <%= f.text_field :texto_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
